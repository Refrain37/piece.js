{
  "name": "piece.js",
  "private": true,
  "version": "0.0.0",
  "description": "frontend frame",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "author": "Refrain",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.26.2"
  }
}
